const input = [
  { x: 3, y: 4 },
  { x: 12, y: 5 },
  { x: 8, y: 15 }
];

// let result = input.map(function(zSide){
//   return (Math.sqrt(zSide.x * zSide.x + zSide.y * zSide.y));
// });

// let result = input.map(zSide => Math.sqrt(zSide.x * zSide.x + zSide.y * zSide.y));

let result = input.map((result) => Math.sqrt(x **2 + y *));


console.log(result[0] === 5);
console.log(result[1] === 13);
console.log(result[2] === 17);